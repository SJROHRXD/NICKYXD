import { ButtonGroup, Button } from "react-bootstrap";

export function SocMedBox() {
  return (
    <ButtonGroup>
      <Button variant="secondary">LinkedIn</Button>
      <Button variant="secondary">Other</Button>
      <Button variant="secondary">Other</Button>
      <Button variant="secondary">Other</Button>
    </ButtonGroup>
  );
}
